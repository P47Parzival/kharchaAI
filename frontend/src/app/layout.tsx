import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "KharchaAI — Know the Real Cost Before You Build",
  description:
    "AI-powered hardware cost estimation. Get accurate, real-time pricing for your prototype components with source citations.",
  keywords: ["hardware", "cost estimation", "BOM", "prototype", "pricing", "AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
